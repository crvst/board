import actionTypes from "actiontypes";

const { CREATE, REMOVE, SORT, SORT_CARDS } = actionTypes(
  "columns",
  "CREATE",
  "REMOVE",
  "SORT",
  "SORT_CARDS",
);

export { CREATE, REMOVE, SORT, SORT_CARDS };
